{
    "custom/power_btn": {
        "format" : "{icon}",
        "format-icons": [""],
        //"on-click": "sh -c'(sleep 0.5s; wlogout --protocol layer-shell)' & disown",
        "on-click": "wlogout --protocol layer-shell -b 5 -H 100",
        "tooltip-format": "Shutdown",
        "tooltip": true
    }
}
